Folder sync is more capable and reliable in this build:

- Added a folder sync workflow for keeping selected source and destination folders paired.
- Sync pairs now persist across launches, including security-scoped folder access.
- Added import and export for sync pair lists.
- Added sync file filters and notification controls.
- Improved handling for shared or same-name destination folders.
